import React from "react";
import { Container, Typography } from "@material-ui/core";

const Dashboard = () => {
  return (
    <Container style={{ marginTop: 40 }}>
      <Typography variant="h4" align="center">
        Bem vindo ao site do Henrique.
      </Typography>
    </Container>
  );
};

export default Dashboard;
